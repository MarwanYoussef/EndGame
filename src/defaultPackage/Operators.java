package defaultPackage;


// Enum for the available operators
// Nodes gets expanded when an operator
// is applied to a node.

public enum Operators {
	
	LEFT,
	RIGHT,
	UP,
	DOWN,
	KILL,
	COLLECT,
	SNAP

}
